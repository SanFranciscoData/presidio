# This directory holds the container-side react_toolbelt_agent package.
# It is a Python package only so the vendored sources ship in the wheel;
# nothing here is imported by presidio host-side code (the vendored agent
# depends on fastmcp, which presidio itself does not).
